pipeline{
    agent any

    environment{
        deploy_to = 'prod1'
    }

    stages{
        stage('when stage'){

            when{

                environment name :'deploy_to' , value : 'prod'
            }

            steps{

                echo "deploying to when stage."
            }
        }
    }
}
